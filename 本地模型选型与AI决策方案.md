# AI 肉鸽游戏 · 本地模型完整选型方案

> 版本：v1.2 ｜ 日期：2026-09-22 ｜ 硬件：RTX 4060 Laptop 8GB / Ryzen 7 7840H / 16GB 内存
> 目标：AI 嵌入游戏本体（干涉游戏底层）+ token 生成速度优先 + 开源可商用 + 全部安装于 D 盘
> v1.1 新增：加入「OpenJev 策略输出头」，用于在引擎搜索前提前合法化/排序候选选择。
> v1.2 新增：把「封闭合法动作集」升级为「可组合安全原语 + 组合校验器」，实现生成—选择分离下的无限动作组合。
> v1.3 新增：决策头统一命名为 OpenJev（独立开源的 decision-native 决策头），与简历/毕设方案措辞对齐。

---

## 一、总览

| # | 决策项 | 选择 | 备选 |
|---|---|---|---|
| 1 | 推理引擎 | **LLamaSharp**（MIT） | llama.cpp server |
| 2 | 模型 | **Qwen2.5-3B-Instruct** + **1.5B**（降级档） | Qwen2.5-7B |
| 3 | 量化 | **Q4_K_M（GGUF）** | Q6_K / Q8_0 |
| 4 | 推理后端 | **CUDA12**（全 GPU） | CPU（低配玩家机） |
| 5 | 上下文 | **ctx=2048 + 外置记忆摘要** | 长上下文硬扛 |
| 6 | 集成架构 | **后台线程 async + 流式输出 + 决策校验** | — |
| 7 | 降级策略 | **3B → 1.5B → 预设文本** 三层 | — |
| 8 | 存储 | **D:\Unity\AI\** 全部 | — |
| 9 | 开发环境 | **Unity 6000.0.28f1c1**（已装） | Unity 2022 LTS |
| 10 | 策略输出头 | **生成—选择分离 + 可组合安全原语（OpenJev 决策头，decision-native）** | 官方 Jev / 纯规则枚举 |

---

## 二、逐项选型与原因

### 1. 推理引擎：LLamaSharp（MIT）
- **为什么**：C# 原生绑定 llama.cpp，可直接嵌入 Unity 进程。设计上 AI 需要干涉游戏底层（直接访问/修改游戏状态），跨进程方案（server）无法做到毫秒级、直接的底层干涉，且需暴露全部内部 API。
- **许可证**：MIT —— 可商用、可修改、可随游戏分发，无需任何授权。
- **能力**：流式输出、CUDA 后端、GGUF 加载、.NET Standard 2.1（Unity 兼容）。
- **备选**：llama.cpp server 仅作开发期快速验证模型用。

### 2. 模型：Qwen2.5-3B-Instruct（正式）+ Qwen2.5-1.5B-Instruct（降级档）
- **为什么 3B**：3B 是"智力/速度"甜点。同系列内智力随参数递增，但生成速度与参数严格反比（见第 3 条公式）。3B 在 4060 上约 80-120 tok/s，一句话 0.5-1.0s，达实时交互门槛。
- **为什么 1.5B**：不是替代，是低配玩家机的降级档（150-250 tok/s），以及开发期快速验证。
- **许可证**：Apache 2.0 —— 商业零限制（Steam 销售完全合法）。
- **中文**：母语级中文对话质量，NPC 角色扮演体验远好于同尺寸 Llama。
- **可扩展**：两者均支持 LoRA/QLoRA 微调（Unsloth 一键），后期可微调成游戏专属人设。

### 3. 量化：Q4_K_M（GGUF）
- **核心公式**：生成速度 ≈ 显存带宽 ÷ 模型权重体积；权重体积 = 参数量 × 每参数量化位数。
- **结论**：4060 带宽 256GB/s 固定，Q4 把体积压到 fp16 的一半 → 速度近翻倍。
- **为什么 Q4_K_M 而非其他**：Q4_K_M 在 Q4 家族中质量损失最小（比 Q4_0 好），速度又远快于 Q6/Q8。
- **体积**：3B ≈ 2GB；1.5B ≈ 1.1GB。

### 4. 推理后端：CUDA12
- **为什么**：4060 全层 GPU 推理（n_gpu_layers 拉满），比 CPU 快 5-10 倍。
- **实现**：LLamaSharp.Backend.Cuda12 NuGet 包。
- **兼容**：同时保留 CPU 后端引用，发布时检测玩家机器，无 NVIDIA 显卡则自动降 CPU。

### 5. 上下文策略：ctx=2048 + 外置记忆摘要
- **为什么**：TTFT（首字延迟）与输入长度成正比——短上下文保首字。肉鸽是长会话，用"记忆摘要压缩"替代无限塞 token。
- **配套**：KV cache 量化（q8_0）再省显存带宽。

### 6. 集成架构：后台线程 + 流式 + 决策校验
```
游戏状态/变量 ─→ [后台线程] LLamaSharp 推理 ─→ 结构化决策指令 ─→ 主线程游戏逻辑执行
     ↑                                                              │
     └──────────────── AI 修改游戏底层状态（闭环）────────────────────┘
```
- **后台线程 async**：推理不阻塞 Unity 主线程，画面永不卡。
- **流式输出**：首字即显示（打字机演出），感知速度 0.3s 级别。
- **决策校验**：AI 输出结构化指令（JSON），规则层校验后执行——既保留底层干涉力，又可兜底防崩溃。

### 7. 降级策略：三层模型
| 层 | 条件 | 模型 |
|---|---|---|
| 正式 | 玩家有 NVIDIA 独显 | 3B（80-120 tok/s） |
| 降级 | 玩家无独显/显存 <4GB | 1.5B（150-250 tok/s，CPU 也行） |
| 兜底 | AI 故障/超时 | 预设文本，游戏流程不中断 |

### 8. 存储：D:\Unity\AI\
- `models/` —— GGUF 模型文件
- 引擎包、测试工程全部 D 盘，不占 C 盘。

### 9. 开发环境：Unity 6000.0.28f1c1
- 已安装（D:\Unity\Unity 6000.0.28f1c1），无需新增。
- LLamaSharp 兼容 Unity（.NET Standard 2.1）。

### 10. GameAgentHarness：生成—选择分离 + 可组合安全原语（OpenJev 决策头）
- **为什么（职责分离）**：LLM 只做「纯软件」两件事——解析玩家对话、生成特殊场景的特别回应文本，**从不直接发出一行游戏命令**。游戏引擎的每次改动都来自合法动作。为避免「封闭枚举动作集」表达力不足，把合法动作升级为**可组合安全原语**：AI 用受约束的组合语法（循环 / 位置 / 条件）把有限原语组合出无限回应，组合结果再由**组合校验器（安全解释器）**统一校验后才交给引擎。**安全始终由设计 + 编译器保证，不依赖模型对规则的自觉。**
- **做法（四段）**：
  1. **合法原语（原子命令，构造保证合法）**：如 `生成(ITEM)@(X,Y)`、`给予(buff)@(单位)`，自带类型、参数范围语义。
  2. **组合语法（受限 DSL）**：循环计数 ≤ 上限、坐标在场景范围内、每帧动作配额、动作互斥、条件守卫（如「进下一房间时」「进入 Boss 战」）。
  3. **组合校验器 / 安全解释器**：解析 AI 输出的组合意图 → 强制约束 → 校验通过才进引擎；越界 / 无守卫 / 伪装命令一律拒绝或按规则降级（例：`循环100次` 被压到配额上限）。合法由**构造 + 编译期**双层保证。
  4. **决策头（OpenJev，decision-native）**：只在候选意图/原语组合上**打分选优**（`plan`），可选 `legalize` 对组合意图做语义前置过滤；它影响「选哪个」，不影响「能否执行」。
- **效果**：有限原语 × 组合语法 → 无限行为多样性 + 对抗式实时反馈。例：「不买」→ 进下一房间触发 `循环生成(火焰符)*100 堆满屏幕`；「Boss 战」→ `场地buff(免疫火焰)`。用**有限合法组合制造无限新鲜感**。
- **模型**：OpenJev 决策头冻结 Qwen3.5-4B（nf4，已在本机 4060 跑通，0.15s/条决策）。decision-native：prompt 只喂「状态 + 候选」，读选项 token 概率，不做文本生成。
- **速度**（官方 3090 实测，4060 预估略高）：同状态复用 prefix 下 37×21 工作负载约 20 decisions/s；一次 21 个二值判据约 1.02s / 0 输出 token；对比自回归 JSON 数组 5.33s（约 5.2× 慢）。组合校验器本身为纯规则/解析，开销忽略不计。
- **部署形态**：跨进程解耦。Unity 主对话引擎（LLamaSharp）+ 独立 sidecar OpenJev 策略服务 + 组合校验器（可并入 sidecar；校验器保持**纯规则、无模型**，保证零 LLM 依赖的兜底）。四段均不占用流式对话管线。
- **许可证**：OpenJev 为独立开源项目（MIT），复用开放权重模型（Qwen，Apache 2.0），不依赖 TypeSafe 官方 Jev（闭源、需 waitlist、不可商用嵌入）。
- **备选**：a) 纯规则层硬合法化——快但无法表达语义优先级、无组合；b) 用本机对话模型生成 JSON 再校验——慢且易崩（5.2× 实测）；c) TypeSafe 官方 Jev——闭源，不适用本方案。

---

## 三、性能目标（估算，4060 实测为准）

| 指标 | 3B (Q4_K_M) | 1.5B (Q4_K_M) |
|---|---|---|
| TTFT 首字延迟 | 150–300 ms | 80–200 ms |
| 生成速度 | 80–120 tok/s | 150–250 tok/s |
| 一句话（30 token）总时 | 0.5–1.0 s | 0.3–0.5 s |
| 权重体积 | ~2 GB | ~1.1 GB |

## 四、速度优化点清单（按影响排序）

1. 全量 GPU 推理（CUDA，n_gpu_layers 拉满）
2. Q4_K_M 量化（带宽减半）
3. 短上下文 2048 + 精简提示词（降 prefill）
4. Flash Attention 开启（省带宽）
5. KV cache 量化 q8_0（省带宽）
6. 流式输出首字即显示（感知速度）
7. 模型常驻内存（不重复加载）
8. 生成参数：max_tokens 限制（一句话 60-100 上限）

## 五、执行计划

| 步骤 | 内容 | 产物 |
|---|---|---|
| 1 | 下载 Qwen2.5-1.5B-Instruct Q4_K_M（1.1GB）→ D:\Unity\AI\models | 模型文件 |
| 2 | 下载 LLamaSharp（NuGet + CUDA12 native 库）→ D:\Unity\AI | 引擎包 |
| 3 | Unity 建工程，集成 LLamaSharp，加载模型 | 可运行项目 |
| 4 | 写测试脚本：游戏内提问 → 后台推理 → 流式返回 → 改写游戏变量 | 链路打通 |
| 5 | 实测 TTFT / tok/s，记录基准 | 性能报告 |
| 6 | 补 3B 模型，同 prompt 实测对比 | A/B 结论 |
| 7 | 定义合法原语集合 + 组合语法，实现组合校验器；部署 OpenJev 决策头 sidecar，建立 localhost 接口 | 原语/校验器/决策服务 |
| 8 | Unity 引擎接入：组合意图 → 校验器强制约束 → 通过后执行；OpenJev 在候选组合上打分选优 | 引擎安全组合链路 |

## 六、待办/风险

- [ ] 模型下载（需代理，走 Clash 127.0.0.1:7890）
- [ ] 确认 Unity 6000.0.28f1c1 许可证激活状态
- [ ] LLamaSharp 与 Unity 版本兼容性实测
- [ ] 风险：LLamaSharp native 库需匹配 Unity 平台（Windows x64 已确认支持）
- [ ] 风险：8GB 显存内，策略头 sidecar（Qwen3.5-4B nf4）与对话模型能否同驻，或需顺序/分时加载——需实测显存占用与切换开销
- [ ] 待办：划定合法原语集合与组合约束（循环/坐标/每帧配额/互斥/条件守卫）的具体上限与语义
- [ ] 待办：组合校验器保持**纯规则、零 LLM 依赖**——注入越界/伪命令时验证「非法零执行率」
- [ ] 待办：确认 Unity 调用独立服务的接口协议与可用性降级（服务未起/超时 → 回退纯规则原语/预设动作）
